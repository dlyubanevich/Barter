spring:
  data:
    mongodb:
      database: barter-photo
      host: localhost
      port: 27017
  application:
    name: photo-microservice

logging:
  level:
    root: INFO

management:
  endpoints:
    web:
      exposure:
        include: "*"

server:
  port: 9007

eureka:
  client:
    fetch-registry: true