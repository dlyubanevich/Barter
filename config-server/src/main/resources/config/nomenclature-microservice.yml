spring:
  data:
    mongodb:
      database: barter-nomenclatures
      host: localhost
      port: 27017
  application:
    name: nomenclature-microservice

logging:
  level:
    root: INFO

management:
  endpoints:
    web:
      exposure:
        include: "*"

server:
  port: 9003

eureka:
  client:
    fetch-registry: true