spring:
  data:
    mongodb:
      database: barter-users
      host: localhost
      port: 27017
  application:
    name: users

logging:
  level:
    root: INFO

management:
  endpoints:
    web:
      exposure:
        include: "*"

server:
  port: 9001

eureka:
  client:
    fetch-registry: true