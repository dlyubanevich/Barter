spring:
  data:
    mongodb:
      host: localhost
      database: library
      port: 0

logging:
  level:
    root: ERROR

eureka:
  client:
    enabled: false

mongock:
  runner-type: "ApplicationRunner"
  change-logs-scan-package:
    - ru.dlyubanevich.user.mongock.changelog